import { FileImage, MoreHorizontal } from 'lucide-react';

const statusTone = { Verified: 'success', Pending: 'warning', Review: 'warning', Suspicious: 'danger', Suspended: 'danger' };

export default function AssetTable({ rows, canManage = false, onStatusChange }) {
	return <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Asset</th><th>Owner</th><th>Category</th><th>Verification score</th><th>Marketplace</th><th>Status</th><th/></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td><div className="admin-asset-cell"><span style={{ '--asset-color': row.color || 'var(--admin-accent)' }}><FileImage size={17}/></span><div><strong>{row.title}</strong><small>{row.id}</small></div></div></td><td>{row.owner}</td><td>{row.category || 'Uncategorized'}</td><td>{row.score === null ? <span className="admin-role-pill">Not scored</span> : <div className="admin-score"><strong>{row.score}%</strong><span><i style={{ width: `${row.score}%` }}/></span></div>}</td><td><span className="admin-role-pill">{row.market || 'Unlisted'}</span></td><td>{canManage ? <select value={row.adminStatus} onChange={(event) => onStatusChange?.(row.id, event.target.value)}><option value="active">Active</option><option value="review">Review</option><option value="suspicious">Suspicious</option><option value="suspended">Suspended</option></select> : <span className={`admin-status is-${statusTone[row.status]}`}>{row.status}</span>}</td><td><button type="button" className="admin-table__more"><MoreHorizontal size={16}/></button></td></tr>)}</tbody></table></div>;
}
